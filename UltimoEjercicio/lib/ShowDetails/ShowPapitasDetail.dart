import 'package:flutter/material.dart';

class ShowPapitasDetail extends StatelessWidget {
  const ShowPapitasDetail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      //ToDo realizar el diseño
    );
  }
}
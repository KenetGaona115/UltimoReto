import 'package:flutter/material.dart';

class ShowPapitas extends StatelessWidget {
  const ShowPapitas({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      //TOdo Hacer la presentacion como la de refrescos
    );
  }
}